import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#0A1D2E', // Beer yellow
    },
    secondary: {
      main: '#2D5B67', // A complementary blue
    },
  },
});

export default theme;